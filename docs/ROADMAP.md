# ROADMAP — Fitness Recomp

## Phase 0 — Fondations ✅
Coquille installable iOS, hors-ligne, qui ne perd pas ses données + sauvegarde/restauration.
- [x] Scaffold React + Vite + Tailwind v4
- [x] PWA installable (manifest + service worker hors-ligne, `vite-plugin-pwa`)
- [x] Dexie.js avec le schéma v1 validé
- [x] Export JSON complet + import/restauration
- [x] `navigator.storage.persist()` au lancement
- [x] Docs `/docs/` créées
- [x] Shell UI (header, tab bar, écran Jour lecture seule, écran Données)

## Phase 1 — Le quotidien utilisable (priorité absolue)
But : **utiliser l'app tous les jours.** Séquence révisée (cadrage 09/06) :

- [x] **1.0 Migration schéma v2** — PK UUID, `updatedAt` partout, `loggedAt`, base compatible sync futur (voir DECISIONS.md D10-D13). ✅ commitée.
- [x] **1.1 Suivi du poids** ✅ commitée (098bb88) — saisie kg + date/heure → `weightLogs` ; courbe de tendance + moyenne glissante (trailing 7 points, D14) ; encart « bon moment pour se peser ». Dashboard Jour = poids du jour + tendance. _(Régression écran Jour blanc corrigée au passage + smoke Playwright committé, 8bed676.)_
- [x] **2. Profil / onboarding + moteur de calcul métabolique** ✅ commitée (de0dc89) — profil (sexe, âge, taille, activité, objectif, %MG?) ; BMR Mifflin-St Jeor / Katch-McArdle, TDEE (5 multiplicateurs), cibles recomp (TDEE×0,90, prot 2,0 g/kg) avec garde-fous codés en dur ; IMC + caveat ; dé-seed des cibles. Calibrage TDEE empirique différé (seam prêt). Voir D15.
- [x] **3. Tickers interactifs** ✅ commitée (6406954) — cocher/incrémenter sur Jour → `tickerStates` keyé par date (absence de ligne = 0 → reset auto minuit, **pas de cron**, D3) ; compteurs − / + bornés à 0, cases en toggle ; progression par ticker (5/8) + compteur de complétion. Logique extraite/testée (`src/lib/tickers.js`).
- [x] **4. Bilan énergétique** ✅ commitée (8739e19 + harden ae947ae) — encart Jour : saisie rapide de la dépense totale du jour → table dédiée `dailyExpenditure` (1 ligne/date, index unique `&date`, absence = non saisi) ; bilan = consommé − dépensé, calculé jamais stocké, consommé honnête tant que la nutrition n'est pas là. Bump `DEXIE_VERSION 2→3` additif + garde-fou wipe réservé v1 (D16).
- [x] **4.5 Consommé rapide du jour** ✅ (D17, commit en attente) — saisie du **total kcal du jour** (1 nombre, 2 s, pas de macros) → table dédiée `dailyIntake` (1 ligne/date, index unique `&date`, absence = non saisi). Seam unique `effectiveConsumed = manualTotal ?? journalSum` (verrou nullish). Bump `DEXIE_VERSION 3→4` additif (D16/D17). Macros « non renseigné » quand le consommé vient du total manuel. Réconciliation définitive avec la nutrition **déférée**.
- [x] **5. Nutrition** ✅ (D18, commit en attente) — écran Bouffe : bibliothèque 58 ingrédients bruts /100 g (recherche + filtres catégorie dynamiques + CRUD) ; composition d'un plat par pesée → `journalEntries` (macros figées D1, sourceType D2, IG copié D5) ; journal du jour (regrammage/suppression). Seed gardé par flag `librarySeededV1`. **Zéro bump Dexie** (champs non indexés). Côté « consommé » du Jour s'allume seul. **Clôt la Phase 1.** _Boissons (~38) déférées en sous-tâche ; réconciliation définitive du consommé **tranchée par D20** (voir inter-phases)._

### Inter-phases — Filtre-recherche du picker Composer ✅ (`9f14939`, poussé)
- [x] Champ « Filtrer les ingrédients » au-dessus du `<select>` du Composer (helper pur `filterIngredients` partagé avec la Bibliothèque, source unique). Picker déroulant conservé, smokes `selectOption` verts par construction (filtre vide ⇒ 58 options).

### Inter-phases — Réconciliation consommé D20 ✅ (commit en attente)
- [x] **D20** (tranche le report de D17) : le **journal prime dès ≥1 entrée**, le total manuel (`dailyIntake`) n'est qu'un **fallback** à 0 entrée. Seam unique `effectiveConsumed(manualTotal, journalSum, entryCount)`, verrou nullish recadré au cas fallback ; bandeau héro « le journal prime » + effacement du total manuel ; `ConsumedRow` unifiée (valeur effective en prop, zéro précédence dupliquée). UI-only, aucun bump Dexie. Test `intake.test.mjs` migré + smoke D20. Voir DECISIONS.md D20.

### Inter-phases — Recettes récurrentes ✅ (D19, commit en attente)
- [x] **Recettes récurrentes** — store `recipes` additif (DEXIE_VERSION 4→5, SCHEMA_VERSION reste 2) ; une recette = formule de **références vivante** (`{ sourceId, nameSnapshot, grams }`, pas de macros figées), rappelable en 1 tap → append au journal du jour via `saveMeal` (macros figées **à l'application**, D1) ; `nameSnapshot` **fallback-only** (affiché seulement si l'ingrédient ne résout plus → ligne sautée + avertissement) ; sous-vue **Recettes** dans le segmented control de Bouffe ; renommage seul (édition des lignes différée) ; « Enregistrer comme recette » depuis le Composer **conserve** les lignes. Test migration **S8** (v4→v5 additif, données préservées) + `recipes.test.mjs` + 2 smokes (nominal + dégradé). Voir D19.

_Parké : tracking micronutriments fin (fer/vitD…) → exige une base Ciqual/USDA, phase dédiée._
_Parké : **Tickers configurables** (cahier des charges, volontairement différé) — l'utilisateur ajoute/retire ses propres tickers et règle label / type / cible / ordre / `active` (les 4 actuels sont seedés et figés). Écran de gestion sur `tickerConfigs` (déjà en base, déjà dans `TABLES`) ; touche à `seed.js` (seed conditionnel) et à l'export. Petite tranche dédiée, après le quotidien utilisable._

## Dette technique
- **Bundle > 500 kB** (warning au build) à cause de **Recharts**. Optimisation = **lazy-load des écrans à graphes** (Poids, puis Perf en Phase 2) via `React.lazy` / import dynamique, pour sortir Recharts du chunk initial. **À faire plus tard** — pas bloquant pour une app 100 % locale, juste tracé pour ne pas le perdre.

## Phase 2 — Intelligence
- [x] **5. Intelligence glucidique (lot 1, granularité JOUR)** ✅ (D21, commit en attente) — `src/lib/glycemic.js` (pur) : composition bas/modéré/haut IG dérivée du journal (grammes de glucides par `gi`, résidu `unknown` au dénominateur jamais au numérateur haut-IG) + 2 règles d'alerte pures (**A** sucres > cible/D15 ; **B** haut-IG jour de repos : `!trained && totalCarb ≥ 50 g && highShare ≥ 0,50`). Contexte séance = saisie explicite 1-tap → table dédiée **`trainingDays`** (présence = séance, absence = repos, bump `DEXIE_VERSION 5→6` additif D16, TABLES/D7 même commit, S9). UI Jour : chip séance + carte composition + pills d'alerte. `effectiveConsumed`/D20 non touché. **Rejet « bas-IG insuffisant » + réconciliation `trainingDays` vs `workouts` DÉFÉRÉE à l'import Strong** (D21). _Timing horaire des glucides autour de la séance différé (exige une heure de séance — voir import Strong)._
- [x] **6. Import CSV Strong** ✅ (D22, commit en attente) — onglet **Perf** : import de l'export CSV Strong (PapaParse) → `workouts`/`sets`. Calé sur l'export RÉEL (`tests/fixtures/strong-export-reel.csv`, 28 séances / 504 séries) : en-têtes **FR+EN** (détection à l'ouverture, erreur claire si inconnu), **filtre structurel** (`Ordre de la série` numérique ; 504 « Minuteur de repos » comptés par libellé), trim exos, coercition réps/poids, **date Safari-safe** (slice + parse par composants, jamais `Date(raw)`). **Idempotence** = index unique `&strongKey` (bump `DEXIE_VERSION 6→7` additif, D16, S10) + skip-on-conflict (replace déféré). **Cardio capté brut** (distance/secondes, aucune estimation calorique). Réconciliation **D21 tranchée (option C)** : `trained` = manuel OU séance importée réelle (étirements seuls exclus), seam `effectiveTrained`. Log consultable (séances → détail exos/séries). **Périmètre v1 = import + log, PAS d'analyse.** Voir D22.
- [x] **7. Analyse de perf** ✅ (D23, commit en attente) — `src/lib/perf.js` (pur, dérivé des `sets`, **zéro stockage, zéro bump Dexie**) : **métrique PAR EXERCICE** (durcissement) — e1RM Epley plafonné à reps ≤ 12 si ≥3 séances à top-set valide, **sinon volume étiqueté** ; **interdit de mélanger** e1RM et volume dans une tendance. Fenêtre **N=3 séances** (jamais calendaire), verdict ↑/→/↓ bande **±2,5 %** (max 2 dernières vs max antérieures → séance molle isolée ≠ régression), **gate « données insuffisantes » < 3 séances** (section repliée, jamais extrapolé). Records all-time dérivés + badge PR. **Échauffement** = paliers `[40%×8, 60%×5, 75%×3, 90%×1]` (constante seam) sur le top-set de la dernière séance, arrondi 2,5 kg, drop palier, plancher 20 kg. **Variantes** = constante statique `src/data/exerciseVariants.js` (PAS d'IA). UI Perf : segmented **`Synthèse | Import`** (log D22 intact), détail exo = sparkline SVG inline + historique + échauffement + variantes. Cardio exclu (label). Groundé sur la fixture réelle (16 suivis / 0 volume naturel / 32 insuffisants / 4 cardio). `perf.test.mjs` (dont **test anti-mélange** synthétique) + smoke. Voir D23.

## Phase 3 — Assistance
- [x] **8. Suggestions de repas (REDÉFINI vs « mini-chat »)** ✅ (D24, `feat(suggest)` poussé) — **le chat est ABANDONNÉ comme surface** (pas de champ texte libre, pas de parsing de mots-clés ; la conversation = couche IA Phase 3, point 9, différée). Surface livrée = **carte « Suggestions » sur l'écran Jour + chips de filtres**. `src/lib/suggest.js` (pur, dérivé, **zéro stockage / zéro bump Dexie**) : source primaire = les **recettes enregistrées** (D19, exactes au gramme, résolues à la volée contre la biblio courante) ; score pondéré **seam unique** (`SUGGEST_WEIGHTS` : protéine prioritaire, proximité kcal au budget visé, coût sucres) ; faisabilité kcal (tolérance 10 %) + sucres simples (borne dure recomp) ; contexte séance (`effectiveTrained` D21/D22) **pondère** le tri (haut-IG ↑ jour d'entraînement, ↓ jour de repos) ; **heure → fraction du budget visée** (`FILL_FRACTION_BY_SLOT`, bornes `SLOT_BOUNDS` exportées) ; **orientation lendemain** (carence protéique de la veille → sur-pondère le protéiné, **jamais le budget**, garde `null` si pas de journal détaillé). **GATE honnête** : rien ne tient → message **chiffré** (jamais un « moins pire » muet) + **complément mono-ingrédient** calculé depuis la biblio (étiqueté « complément », pas « repas »), **élargi à l'état sous-seuil** si gap protéique ≥ 15 g (le cas du soir). **Chips dérivées v1, zéro bump** (Protéiné / Léger / Post-séance — `tags`/type-de-plat différés). **Mode dégradé D20** kcal-only (macros consommées inconnues → aucune affirmation macro). UI : carte sous le bloc restant, **1 tap = `applyRecipe` (flux D19 réutilisé)** → recharge → re-tri. `suggest.test.mjs` (14 blocs : gate chiffré, sucres liants, séance, carence veille, complément couvre/borne/null, kcal-only, sous-seuil ±gap, chips, câblage veille `todayKey`) + smoke. Voir D24.
9. Couche IA Claude **optionnelle**, opt-in, désactivable — interface commune moteur de règles / moteur IA prévue dès la Phase 3 (implémentation différée). _Inclut désormais : mini-chat repas conversationnel + import de recette par texte collé (légende Insta/TikTok) — déférés ici comme cas d'usage IA._

## À recréer (ancien projet perdu — pas de migration)
- Base ~38 boissons alcoolisées (kcal + portions standard).
- Logique d'intelligence glucidique.
- Bibliothèque d'ingrédients bruts de départ (/100 g), enrichissable.
