# DECISIONS — Fitness Recomp

Choix d'architecture et leur justification. Append-only.

## D1 — Macros figées (snapshot) dans `journalEntries`
Les valeurs kcal/protéines/glucides/sucres/lipides sont **copiées au moment de la saisie**, pas recalculées à la volée depuis l'ingrédient.
**Pourquoi** : éditer ou supprimer un ingrédient ne doit pas réécrire l'historique des journées passées. L'intégrité du suivi prime sur la normalisation.

## D2 — Boissons dans le même journal via `sourceType`
`journalEntries.sourceType ∈ {ingredient, drink}` + `sourceId`. Un verre compte directement dans les kcal/sucres du jour.
**Pourquoi** : un seul chemin d'agrégation pour le bilan du jour ; évite une table de log parallèle et une double logique. Alternative (table séparée) rejetée comme plus complexe.

## D3 — Tickers : état keyé par date
`tickerStates` = une ligne par (ticker, jour). **L'absence de ligne pour aujourd'hui = valeur 0.**
**Pourquoi** : reset journalier automatique à minuit **sans tâche planifiée ni cron** — robuste hors-ligne, cohérent avec le 100 % local.

## D4 — IDs auto-incrément numériques (pas d'UUID)
**Pourquoi** : mono-utilisateur, mono-appareil. Suffisant ; l'import en mode `replace` réécrit l'ensemble, pas de collision multi-device à gérer au V1.

## D5 — IG à 3 niveaux (`low/mid/high`)
Le prompt disait « bas/haut » mais la maquette affiche « IG bas/modéré ». Retenu **3 niveaux**.
**Pourquoi** : reflète l'intention réelle de l'intelligence glucidique (modéré ≠ haut autour des entraînements).

## D6 — Import = remplacement intégral (replace), pas fusion
`importBundle` fait `clear()` + `bulkPut` par table, avec garde de `schemaVersion`.
**Pourquoi** : c'est une **restauration de sauvegarde**, sémantique attendue au V1. La fusion (merge) introduirait des conflits d'id non triviaux pour aucun bénéfice mono-appareil.

## D7 — `TABLES` source unique de l'export/import
Le tableau `TABLES` dans `src/db.js` pilote export, import et comptage.
**Pourquoi** : applique mécaniquement la contrainte dure « toute donnée dans l'export ». Ajouter une table sans l'ajouter à `TABLES` = trou de sauvegarde silencieux → règle inscrite dans CLAUDE.md.

## D8 — Tailwind v4 via `@tailwindcss/vite` (pas de tailwind.config.js)
**Pourquoi** : v4 est config-less par défaut, scan automatique. Les tokens couleur de la charte vivent dans `src/ui.js` (objet `C`) et en styles inline, pas dans un thème Tailwind — la maquette était déjà construite ainsi.

## D9 — Emplacement repo : `~/projects/fitness-recomp` (WSL natif)
**Pourquoi** : I/O npm/vite nettement plus rapides qu'en `/mnt/c`. Accès Windows si besoin via `\\wsl$`.

---
_Cadrage « local-first + sync futur » (Tâche 1.0). On reste 100 % local / hors-ligne / zéro backend aujourd'hui ; on rend seulement le schéma compatible avec un sync multi-appareils futur (voie C), pendant que la base est quasi vide. Le sync n'est PAS construit._

## D10 — Clés primaires en UUID (`crypto.randomUUID()`) sur toutes les tables
PK = `id` string UUID, assigné par l'app à l'insert (plus d'auto-incrément `++id`).
**Pourquoi** : éviter les collisions d'ID au merge multi-appareils quand le sync arrivera ; bonus immédiat → supprime tout remapping d'ID à l'import. `crypto.randomUUID()` exige un secure context (OK en HTTPS / localhost). Tombstones / soft-delete : **différés** à la phase sync.

### D10-bis — `settings` : clé sentinelle fixe `'singleton'` (pas un UUID aléatoire)
**Pourquoi** : `settings` est un singleton ; une clé connue et stable permet un `db.settings.get('singleton')` déterministe. Satisfait « PK string » sans casser la lecture.

### D10-ter — FK orpheline (chemin import) : drop + log + comptage
Une ligne référençante dont le parent est introuvable (après remap) est **supprimée**, loggée, et le total est remonté à l'UI.
**Pourquoi** : un pointeur mort est pire qu'une absence ; mieux vaut une perte visible (comptée) qu'une FK cassée silencieuse.

## D11 — Champ `updatedAt` (epoch ms) sur chaque enregistrement
Estampillé à chaque write applicatif via les helpers `newRow()` / `touch()` (`src/db.js`) — **pas** via un hook Dexie global.
**Pourquoi** : socle d'un merge « last-write-wins » futur. Le choix « helper, pas hook » est délibéré : **l'import doit préserver l'`updatedAt` du fichier** (un hook global le réécrirait à `now` au `bulkPut`, détruisant l'horodatage source nécessaire au merge).

## D12 — Champ `loggedAt` sur `journalEntries`
Heure réelle du repas (epoch ms), défaut = `createdAt`, éditable plus tard.
**Pourquoi** : nécessaire à l'intelligence glucidique (timing) et au chat repas à venir ; gratuit à ajouter à vide maintenant, vraie migration plus tard.

## D13 — Migration v1→v2 LIVE = WIPE + RESEED (pas de transform destructif)
Au boot, si une base v1 (PK auto-incrément) est détectée : **backup durable automatique** (IndexedDB séparée `fitnessRecompBackups`) **avant** tout delete → `Dexie.delete` → `db.open()` recrée une base v2 propre → reseed.
**Pourquoi** : (1) Dexie interdit le changement de PK en place (`UpgradeError: Not yet support for changing primary key`) → il faut recréer le store ; (2) mes données live = seed + tests, **jetables** ; le backup couvre le risque. Détection via **ouverture dynamique** Dexie (`probe.verno`), pas via un schéma v1 déclaré (ne lève pas de VersionError fiable). `transformV1toV2()` (remap FK parents→enfants) existe et est testé mais sert **uniquement à l'import de backups v1**, jamais en destructif sur la base live.

## D14 — Moyenne glissante du poids = trailing sur N **points** (pas N jours calendaires)
`movingAverage(values, window=7)` (`src/lib/weight.js`) moyenne les `window` dernières pesées **enregistrées**, indépendamment des dates.
**Pourquoi** : simple, déterministe, testable (valeurs exactes en unit test), suffisant tant que la pesée est ~quotidienne. **Caveat assumé** : si des pesées sont sautées, ces N points couvrent plus de jours réels → tendance bruitée / en retard. Le lissage par **fenêtre calendaire** (7 jours réels, interpolation des trous) est **volontairement reporté** — à revisiter quand l'usage réel le justifiera, pas avant. Inscrit aussi en commentaire dans le code.

## D15 — Politique de cibles recomp + garde-fous métaboliques (codés en dur)
Le moteur `src/lib/metabolic.js` calcule les budgets de toute l'app depuis le profil. Valeurs actées :
- **BMR** : Mifflin-St Jeor par défaut ; Katch-McArdle **uniquement si %MG saisi** (sinon indisponible, faute de LBM fiable).
- **TDEE** : 5 multiplicateurs d'activité — 1,2 / 1,375 / 1,55 / 1,725 / 1,9.
- **Recomp (objectif soigné)** : calories = **TDEE × 0,90** (léger déficit) ; **protéines 2,0 g/kg** (hautes, préservation/gain musculaire) ; **lipides 0,8 g/kg** ; **glucides = reste** des calories ; sucres simples **< 20 g/j** (déjà acté). Perte (×0,80 / 2,2 g/kg) et prise (×1,10 / 1,8 g/kg) prévues mais non soignées.
- **Garde-fous (jamais franchis, quel que soit le profil)** : plancher calorique absolu **♂ 1500 / ♀ 1200 kcal** ; **déficit plafonné à −20 %** (jamais sous TDEE×0,80) ; **plancher protéines 1,6 g/kg** ; **plancher lipides 0,6 g/kg** ; **glucides jamais négatifs** (si prot+lip plancher dépassent le budget, on remonte les kcal).

**Pourquoi** : la recomp exige protéines hautes + énergie quasi maintenance ; les garde-fous empêchent toute cible dangereuse (sous-alimentation, lipides trop bas pour la santé hormonale) sur un profil extrême. **Codés en dur volontairement** (pas seulement affichés) : le moteur ne PEUT pas émettre une cible sous ces planchers — testé par balayage (`tests/metabolic.test.mjs`).
**Seam d'évolution** : tout passe par `computeTargets()` et le TDEE est stocké → un futur **calibrage empirique** (ajuster le TDEE selon l'évolution réelle du poids) s'insère à un seul endroit, sans refonte. Volontairement **différé**, porte laissée ouverte (cf. ROADMAP Tâche 2, point 11).

## D16 — Versioning Dexie additif à partir de v2 + wipe réservé aux bases pré-UUID

**INVARIANT DUR.** À partir de v2 (PK UUID), toute montée de `DEXIE_VERSION` est **ADDITIVE** : on ajoute un store ou un index via une nouvelle `db.version(n).stores(delta)` ne déclarant QUE le delta (Dexie conserve les stores antérieurs). Une telle montée ne déclenche **JAMAIS** le wipe legacy. Le wipe destructif (D13) reste réservé aux bases **PRÉ-UUID** (v1, PK auto-incrément), détecté par `verno < FIRST_UUID_DEXIE_VERSION` dans `migrateLegacyIfNeeded()`. Verrouillé par le test **S5** de `migration.test.mjs` (base v2 réelle sur disque → bump v3 → données préservées, nouveau store créé vide, aucun backup de wipe).

**Pourquoi** : le bump Tâche 4 (ajout de `dailyExpenditure`) avec l'ancienne détection (`verno < DEXIE_VERSION` ⇒ legacy) aurait wipé **toute base v2 du device** → perte des vraies données. La détection legacy doit cibler la **rupture de PK** (auto-incrément → UUID), pas « toute version inférieure à la courante ».

**Corollaires actés**
- `SCHEMA_VERSION` (format d'export JSON) est **DÉCOUPLÉ** de `DEXIE_VERSION` : il ne bouge qu'à une rupture de **format** d'export, pas à chaque ajout de store. Resté à **2** ici — ajouter une table est rétro-compatible à l'import (table absente d'un vieux bundle ⇒ reste vide). **Garde** : test **S6** (`migration.test.mjs`) prouve qu'un bundle v2 sans `dailyExpenditure` + une table inconnue s'importe sans throw → c'est la tolérance de l'importeur qui *autorise* à figer SCHEMA_VERSION, pas une simple affirmation.
- `replace` (import) = **restauration intégrale atomique** : une **seule** `db.transaction('rw', db.tables, …)` vide TOUTES les tables PUIS réinsère ce que le bundle contient. Rollback Dexie si un write jette (ex. violation `&date`) → **jamais de base à moitié wipée**. Raffine **D6** (qui ne vidait que les tables présentes). Une table absente du bundle finit vide, pas avec des résidus du device.
- Dépense énergétique du jour en **table dédiée** `dailyExpenditure` (1 ligne par date, `{ id, date, kcal, updatedAt }`, absence = non saisi). Pas dans `settings` (singleton). « 1 ligne/date » **verrouillé structurellement** : index **unique `&date`** + upsert atomique en transaction rw (`expenditure.js`) — prouvé par un test de double-write concurrent. Le bilan (consommé − dépensé) reste **calculé, jamais stocké**.

## D17 — Consommé total du jour en saisie rapide (`dailyIntake`) + réconciliation DÉFÉRÉE

Saisie rapide du **consommé TOTAL du jour** (1 nombre, **pas de macros**) en **table dédiée `dailyIntake`** (`{ id, date, kcal, updatedAt }`, **1 ligne/date, index unique `&date`, absence = non saisi, JAMAIS 0**). Symétrique de `dailyExpenditure` (D16). Bump **`DEXIE_VERSION 3→4` ADDITIF** (D16 s'applique tel quel : aucun wipe, store créé vide — verrouillé par le test **S7** de `migration.test.mjs`). `SCHEMA_VERSION` **reste 2** (table additive, rétro-compat import). Ajoutée à `TABLES` → export/import couvert (D7). UI : la ligne « Consommé » du Bilan devient éditable (calque exact de la ligne Dépense) ; saisie en 2 s après la séance.

**Réconciliation avec le futur journal nutrition = PROVISOIRE, pas un invariant.** Tant que la nutrition n'a pas de saisie, le consommé effectif passe par un **seam unique** `effectiveConsumed(manualTotal, journalSum)` (`src/lib/intake.js`) : **le total manuel prime** (`manualTotal ?? journalSum`). **Verrou nullish OBLIGATOIRE** : `0` est un total **réel** (il prime), seuls `null`/`undefined` retombent sur la somme du journal — `loadIntake(absent)` renvoie `null` (jamais `0`), effacer **supprime la ligne** (≠ set 0). La règle **définitive** (override vs somme+ajustement, voire **inputs mutuellement exclusifs par date**) sera **tranchée au chantier nutrition**. Le `??` n'existe **qu'à cet endroit** — aucune logique D17 ailleurs.

**Pourquoi** : besoin immédiat « après ma séance, entrer mes kcal du jour en 2 s » sans attendre le gros chantier nutrition. **Table dédiée** (pas un `journalEntry` spécial qui violerait D1/D2 `sourceType ∈ {ingredient, drink}`) → orthogonale au journal, **rien à arracher** quand la nutrition arrivera : le côté « consommé » basculera alors sur `journalEntries`, ce seam tranchant la cohabitation. Macros affichées **« non renseigné »** (pas `0`) quand le consommé vient du total manuel : honnête, pas de faux zéros sur les barres P/C/L ni sur les sucres.

## D18 — Nutrition : zéro bump Dexie + seed gardé par flag + ids slug + regram-from-snapshot + boissons déférées

Lot Nutrition (clôt la Phase 1) : bibliothèque d'ingrédients bruts /100 g + composition d'un plat par pesée → `journalEntries`. Choix actés :

- **ZÉRO bump Dexie** (`DEXIE_VERSION` reste **4**, `SCHEMA_VERSION` reste **2**). Les stores `ingredients`/`journalEntries`/`drinks` existent depuis v2 et leurs index (`name, category` / `date, [date+sourceType], sourceId` / `name, category`) suffisent pour la liste, la recherche, le filtre et le journal du jour. Les valeurs /100 g (`kcal100, protein100, carb100, sugarsSimple100 ⊂ carb100, fat100, gi, isCustom, createdAt`) et les snapshots de `journalEntries` sont des champs **NON INDEXÉS** → écriture libre, aucun `db.version()`. **D16/D17 ne s'appliquent pas** (ils encadrent l'ajout de *stores* ; ici aucun). `backup.js` **intouché** (les 3 tables sont déjà dans `TABLES`, D7) → contrainte dure « toute donnée dans l'export » déjà satisfaite. _Un bump n'aurait été requis que pour un NOUVEL index._

- **Seed bibliothèque gardé par un flag `settings.librarySeededV1`** — PAS par `settings.count()` (le device est déjà initialisé → le seed ne partirait jamais) NI par « table vide » (re-seed après un backup légitimement vide). Le flag vit **dans `settings`**, donc il **voyage avec l'export/import** : un backup pris après le seed porte `librarySeededV1:true` → ré-import = pas de re-seed même si l'utilisateur a vidé sa biblio. `seedLibraryIfNeeded()` (idempotent) tourne au boot après `seedIfEmpty()`.

- **Ids slug stables** pour les 58 ingrédients seedés (`riz-blanc-cuit`, …) plutôt que des UUID aléatoires. **Déterministes** → identiques sur tout device, pas de doublon au futur sync (meilleur que D10 pour de la **donnée de référence**, pas de la donnée utilisateur). Les ingrédients **custom** (créés par l'utilisateur) restent en UUID via `newRow()`. `createdAt` estampillé à l'insertion (`null` dans le fichier seed).

- **Regrammage = rescale du PROPRE snapshot de l'entrée, JAMAIS relecture de l'ingrédient** (`regramMacros`). Renforce **D1** : éditer le grammage d'un repas passé garde la densité nutritionnelle figée à la saisie, même si l'ingrédient source a changé ou été supprimé depuis (prouvé en test, ingrédient supprimé). Supprimer un ingrédient **ne cascade pas** sur l'historique.

- **3 nouvelles catégories** (`fruits`, `laitages`, `aromates`) gérées **dynamiquement** (`distinctCategories` : connues dans un ordre stable puis custom en alpha) — jamais codées en dur dans les filtres.

- **Boissons déférées** : la table `drinks` reste en place mais le lot ne seed ni n'expose les ~38 boissons (ancien projet perdu, pas de source fiable → pas de fabrication de données). `sourceType:'drink'` (D2) non utilisé ici. Recréation = sous-tâche dédiée.

**Pourquoi** : capter la nutrition par pesée (le besoin réel) sans toucher au schéma ni au backup, en respectant D1/D2/D5/D7/D10-D12. Le côté « consommé » du bilan (Tâche 4/4.5) s'allume **seul** dès que `journalEntries` se remplit (déjà câblé), sauf total manuel D17 qui prime. **L'intelligence glucidique reste en Phase 2** : ici on se contente de capter `sugarsSimple100` + `gi` dans le schéma. La règle définitive de réconciliation « consommé » (D17 provisoire) sera tranchée quand l'usage le justifiera.

## D19 — Recettes récurrentes : store `recipes` additif (v5) + formule de RÉFÉRENCES vivante (≠ snapshot) + nameSnapshot fallback-only

Recette réutilisable = composition mémorisée, rappelable en 1 tap dans le journal du jour. Choix actés :

- **Store dédié `recipes` (DEXIE_VERSION 4→5), `SCHEMA_VERSION` reste 2.** Montée **purement ADDITIVE**, pattern **delta-only** identique à v3/v4 : une ligne `db.version(DEXIE_VERSION).stores({ recipes: 'id, name' })` partageant la constante (Dexie **fusionne** les `.stores()` d'une même version — on ne redéclare PAS les 11 stores existants), **aucun callback upgrade**. D16 s'applique tel quel : un device v4 réel n'est **jamais wipé**, le store est créé vide, données préservées — **verrouillé par le test S8** de `migration.test.mjs` (porte dure avant tout déploiement device). `TABLES += 'recipes'` **dans le même commit** (D7, sinon trou de sauvegarde). `backup.js` intouché (piloté par `TABLES`). `SCHEMA_VERSION` figé à 2 = rétro-compat import (table absente d'un vieux bundle ⇒ vide, tolérance prouvée S6).

- **Une recette = formule de RÉFÉRENCES vivante, PAS un snapshot.** Stockée comme `lines: [{ sourceId, nameSnapshot, grams }]` — des références, **aucune macro figée dans la recette**. À chaque « Rappeler », `resolveRecipe` **re-résout** chaque `sourceId` contre la bibliothèque **courante** → nom + valeurs /100 g à jour. Le figeage **D1** n'arrive **qu'à l'application** au journal, via `saveMeal` (qui copie les macros calculées). Distinction nette : `journalEntries` = figé (D1) ; `recipes` = template vivant. Conséquence voulue : éditer un ingrédient (renommage, correction de macros) **se reflète** au prochain rappel, sans réécrire l'historique déjà journalisé.

- **`nameSnapshot` = FALLBACK D'AFFICHAGE UNIQUEMENT.** Tant que l'ingrédient résout, l'UI montre **toujours** le nom **courant** (un renommage n'affiche jamais un nom périmé). Le `nameSnapshot` n'est lu **que** lorsque la résolution échoue (ingrédient supprimé) : la ligne est **sautée** et remontée dans un avertissement « *… — supprimé, ignoré* ». Cas **toutes lignes mortes** → **aucune** `journalEntry` écrite (pas de repas vide). Prouvé par `recipes.test.mjs` (R3 une ligne morte, R4 toutes mortes) + smoke dégradé.

- **« Rappeler » = APPEND, jamais remplacement** (`applyRecipe` → `saveMeal` en `bulkAdd`). On reste sur la sous-vue Recettes avec un **bandeau de retour** (succès / avertissement) — pas de bascule vers Journal (sinon l'avertissement serait démonté).

- **Édition différée** : seul le **renommage** (`renameRecipe`) est supporté. Changer les lignes d'une recette = la **supprimer + re-save** depuis le Composer (`updateRecipe` complet volontairement non construit).

- **« Enregistrer comme recette » depuis le Composer = action annexe** : les lignes du Composer **RESTENT** en place après l'enregistrement (on peut vouloir aussi loguer ces lignes aujourd'hui). Distinct de « Enregistrer le repas » (qui vide + bascule Journal).

- **Écran** : sous-vue **Recettes** via le segmented control en haut de Bouffe (`Composer | Journal | Bibliothèque | Recettes`) — pas une section sous la Bibliothèque, pas une destination de la nav du bas. Réutilise le pattern de liste existant.

- **Recettes = INGRÉDIENTS seulement.** Boissons toujours déférées (D18) ; pas de cas boisson-dans-recette (`saveMeal` n'écrit que `sourceType:'ingredient'`).

**Pourquoi** : besoin réel « je remange souvent les mêmes plats, je veux les rappeler en 1 tap » sans dupliquer la saisie. La formule vivante (références) bat le snapshot figé pour un **template** : corriger un ingrédient profite à tous les rappels futurs. Le figeage reste là où il doit être — **dans le journal** (D1) — pas dans le template. Store additif orthogonal : rien à arracher, aucun invariant existant rouvert (D1/D2/D5/D7/D10-D12/D16/D18 tous respectés).

## D20 — Consommé : le JOURNAL est source de vérité dès ≥1 entrée ; le total manuel (`dailyIntake`) n'est qu'un FALLBACK (tranche le report de D17)

**Règle.** `effectiveConsumed` passe de `manualTotal ?? journalSum` (manuel prioritaire) à **journal prioritaire dès qu'il existe ≥1 entrée** : `entryCount > 0 ? journalSum : manualTotal`. Le total kcal saisi à la main (`dailyIntake`, D17) ne sert plus que les jours **sans aucune entrée de journal** (saisie rapide post-séance avant toute composition). Dès le 1ᵉʳ repas logué dans Bouffe, le journal prime et les macros P/C/L + sucres s'allument depuis le journal (fin des « — »).

**Pourquoi.** D17 avait explicitement **déféré** la règle de réconciliation (« override vs somme+ajustement, voire inputs mutuellement exclusifs par date — tranchée au chantier nutrition »). La nutrition est livrée (D18/D19) : le journal détaillé est désormais la donnée riche (macros, IG, sucres) ; le total manuel n'est qu'un raccourci dégradé. Faire primer le détail sur le raccourci est l'arbitrage cohérent. Le symptôme observé (« le tableau du Jour ne se met pas à jour après un repas logué ») était l'ancienne précédence D17, **pas un bug de code** (Jour relit bien `journalEntries` au remontage).

**Ce que ça NE rouvre PAS.** D17 tient sur tout le reste : table dédiée `dailyIntake` (≠ `journalEntry`, ne viole pas D1/D2), **1 ligne/date** index unique `&date`, **absence = non saisi** (`null`, jamais 0). Le **verrou nullish survit** mais ne s'applique plus qu'au **cas fallback (0 entrée)** : à 0 entrée, un total manuel de `0` reste un 0 réel ; avec des entrées, le manuel (0 compris) est ignoré. Le seam reste **unique** (`effectiveConsumed`) : le `??` est remplacé par la garde `entryCount > 0` **au même endroit** — aucune autre expression de précédence dans l'app (la ligne « Consommé » du Bilan reçoit la valeur effective en prop, elle ne la recalcule pas).

**Visibilité (anti-confusion).** Quand un total manuel existe pour le jour ET que le journal a des entrées (le manuel est donc ignoré), le héro Jour affiche « *Total manuel (X kcal) saisi — le journal prime* » + un tap « **Effacer le total manuel** » qui réutilise `clearIntake` (l'affordance d'effacement existante). Pas de switch, pas de mode, pas de nouvel état/flag.

**Périmètre.** **UI/logique seulement, AUCUN bump Dexie** (`DEXIE_VERSION` reste 5, `SCHEMA_VERSION` reste 2), `dailyIntake` et `backup.js` intouchés → **zéro risque migration**. Verrouillé par `intake.test.mjs` migré (flip « journal prime ≥1 entrée » + « manuel fallback à 0 entrée » + verrou nullish recadré) + smoke D20 (journal injecté 600 + manuel 2100 → héro & Bilan montrent 600, effacement du total manuel).

## D21 — Intelligence glucidique v1 (granularité JOUR) : analyse dérivée du journal + contexte séance explicite (`trainingDays`) + 2 règles d'alerte pures

Phase 2, 1ᵉʳ lot. **Tout est jour-level** : pas de timing horaire des glucides autour de la séance (différé — exige une heure de séance qu'on n'a pas ; le cahier des charges le prévoit, on ne le construit pas ici). Choix actés :

- **Analyse glucidique = ZÉRO nouvelle donnée.** La composition bas/modéré/haut IG se dérive du journal du jour : chaque `journalEntry` porte déjà `carb` (g) + `gi ∈ {low,mid,high}` FIGÉS à la saisie (D5/D18). `src/lib/glycemic.js` (pur) : `carbsByGi` (ventilation des grammes par IG), `glycemicShares` (parts en %), `evaluateGlycemicAlerts` (alertes). Rien n'est stocké — pur calcul, recalculé au montage du Jour.

- **Pondération = GRAMMES DE GLUCIDES par niveau d'IG.** L'IG qualifie le glucide → la composition se mesure en g de carb par bucket (une entrée ~0 g carb pèse ~0, auto-neutralisée). Les **sucres simples sont un axe SÉPARÉ** (règle A), jamais mélangés à la composition IG.

- **RÈGLE DÉNOMINATEUR / NUMÉRATEUR du résidu `unknown`** (arbitrage exigé). `gi` absent ou hors {low,mid,high} → bucket défensif `unknown` (D18 le copie toujours ; on ne crashe jamais sur une donnée legacy). Ses grammes comptent au **DÉNOMINATEUR** (`totalCarb` = glucide réel) mais **JAMAIS au numérateur haut-IG** : `highShare = high / totalCarb`. Un gramme non classé **dilue** la part haut-IG, il ne la gonfle jamais. Documenté dans `glycemic.js` + assertion dédiée (`glycemic.test.mjs` : high=40 / unknown=60 → highShare 0,4 → PAS d'alerte B).

- **Contexte « séance ce jour » = saisie EXPLICITE 1-tap, table dédiée `trainingDays`.** Pas d'inférence (l'import Strong est une phase ultérieure ; `dailyExpenditure` est un total journalier inexploitable pour inférer une séance). Bump **`DEXIE_VERSION 5→6` ADDITIF** (D16 s'applique tel quel, delta-only, aucun wipe — verrouillé par **S9** de `migration.test.mjs`). `SCHEMA_VERSION` **reste 2** (table additive, rétro-compat). `TABLES += 'trainingDays'` **même commit** (D7) ; `backup.js` intouché (piloté par `TABLES`). Ligne `{ id, date, updatedAt }`, index unique `&date` : **PRÉSENCE = séance, ABSENCE = repos** (calque D3/D17 — on ne stocke jamais `false`, **untoggle = suppression de la ligne**). `src/lib/training.js` calque `expenditure.js`/`intake.js` (upsert atomique rw).

- **Set d'alertes MINIMAL = 2 règles, chacune pure et testable** (seam unique `evaluateGlycemicAlerts`, calque `effectiveConsumed`/D20 — **aucune autre expression de seuil ailleurs dans l'app**) :
  - **A « sucres-high »** : `sugars > settings.targetSugarsSimple` (D15, défaut 20 g ; borne STRICTE → 20 ne déclenche pas, 21 oui).
  - **B « high-gi-rest »** : `!trained && totalCarb ≥ 50 g && highShare ≥ 0,50`. Le haut-IG se justifie **autour de la séance** ; sur un jour de **repos**, >50 % des glucides en haut-IG est le signal actionnable. Plancher **50 g** → ne crie pas sur une journée quasi sans glucides. Bornes (≥) prouvées en test.
  - **Rejet acté** de « bas-IG insuffisant » : le bas-IG n'est pas une cible à atteindre → règle bruyante et faiblement fondée. **Différée, pas oubliée** (réinscriptible si l'usage le justifie).

- **UI sur le Jour uniquement** : chip **« Séance aujourd'hui »** (toggle 1-tap, maj optimiste + persistance) ; carte **« Composition glucidique »** (barre 3 segments bas/modéré/haut + % ) affichée **dès ≥1 entrée** (à 0 entrée le détail macros est inconnu, cf. D20 `consumedFromManual`) ; alertes A/B en pills sous la carte. **Rien sur Bouffe** (pas de badge IG par ligne en v1 — hors périmètre acté). `effectiveConsumed` (D20) **NON touché**.

- **RÉCONCILIATION `trainingDays` vs `workouts` DÉFÉRÉE** (report explicite, loggé comme D17 l'avait été — pas un trou). Quand l'**import Strong** arrivera (Phase 2, point 6), une séance pourra venir de deux sources : le toggle manuel (`trainingDays`) OU une séance importée (`workouts`). La règle définitive — **probablement `trained = présence manuelle OU séance importée ce jour`** — sera tranchée **au chantier import Strong**, pas avant. En v1, `trained` = présence d'une ligne `trainingDays` uniquement.

**Pourquoi.** La nutrition livrée (D18/D19) capte déjà `gi` + `sugarsSimple` figés → l'analyse glucidique est **gratuite côté données**, il ne manquait que le calcul + l'affichage. Le seul invariant nécessaire est le **contexte séance** (règle B), capté par une saisie explicite honnête (absence = repos) plutôt qu'une inférence fragile. Table additive orthogonale : rien à arracher, aucune décision D1→D20 rouverte (D3/D5/D13/D16/D17/D18/D20 tous respectés). Verrouillé par `glycemic.test.mjs` (bucketing, parts, règle unknown, bornes A/B, garde `trained`), `training.test.mjs` (présence/absence, upsert idempotent, untoggle, concurrent → 1 ligne), `migration.test.mjs` **S9** (v5→v6 additif, données préservées), et un smoke (compo 80 % haut-IG + 2 alertes jour de repos → toggle séance coupe B → reload persiste).

**ADDENDUM (tranché à D22, import Strong) — la réconciliation différée ci-dessus est résolue : OPTION C.** `trained` (règle B) = **présence manuelle (`trainingDays`) OU séance importée RÉELLE ce jour-là**. Une séance importée **purement échauffement/étirement** (denylist `WARMUP_WORKOUT_NAMES`, seed `"Échauffement / Étirements"`, normalisée trim+casse, constante exportée) **NE compte PAS** comme séance : un jour de mobilité pure est métaboliquement un jour de **repos**, donc l'alerte haut-IG doit **rester active** (la museler serait l'erreur). Le toggle manuel **prime toujours** pour activer (override). Options A (substantive = poids>0 OU cardio) **rejetée** (musellerait une vraie séance au poids du corps) et B (toute séance importée compte) **rejetée** (silence un jour quasi-repos). Seam unique `effectiveTrained({ manualPresent, importedWorkouts })` (`src/lib/training.js`) — aucune autre expression de précédence `trained` ailleurs. Verrouillé par `strongImport.test.mjs` (bloc T) + smoke (déjà couvert via toggle manuel).

## D22 — Import CSV Strong : `strongKey` (idempotence), bump v7 additif, date Safari-safe, filtre structurel

Phase 2, point 6. Import de l'export CSV Strong (musculation/cardio) → tables existantes `workouts`/`sets` (prévues pour ça depuis v2). **Calé sur l'export RÉEL** (`tests/fixtures/strong-export-reel.csv`, 1008 lignes, 28 séances), pas sur le format anglophone documenté en ligne. Choix actés :

- **Périmètre v1 = import + dédup + log consultable.** Onglet **Perf** (existait en Placeholder) : bouton import (PapaParse), rapport (séances ajoutées / déjà importées / séries / cardio / lignes ignorées par libellé), erreur fichier invalide **sans aucun write**, liste des séances repliable (exos + séries). **AUCUNE analyse de perf** (progressions/records = point 7, chantier suivant ; le schéma la permet via l'index `[exercise+date]`). **AUCUNE estimation calorique** : le cardio (Cycling/Walking/Running/Rowing) est **capté BRUT** (`distance`, `seconds`) pour servir un futur fallback énergétique — la D20 (total manuel) **primera toujours**.

- **Schéma `workouts`** : `{ id(UUID), strongKey, date, name, source:'strong-import', durationRaw, startedAt, createdAt?, updatedAt }`. **Schéma `sets`** : `{ id(UUID), workoutId, date, exercise, setIndex, reps, weightKg, distance, seconds, modality:'strength'|'cardio', rpe, updatedAt }`. `durationRaw` = string Strong **brute** (« 1h 39min », **jamais parsée** — l'export réel contient des outliers « 47h 7min » inexploitables ; un commentaire l'interdit). RPE/Notes vides partout dans l'export réel → captés si présents, **rien construit dessus**.

- **IDEMPOTENCE = clé naturelle `strongKey`** (le timestamp `Date` brut, identité de séance répétée sur chaque ligne). **Bump `DEXIE_VERSION 6→7` ADDITIF** = ajout de l'**index unique `&strongKey`** sur le store **existant** `workouts` (≠ nouveau store ; Dexie fusionne les `.stores()` d'une même version → redéclarer `workouts` avec sa liste d'index complète remplace la déclaration de base). Verrou structurel anti-doublon (calque `dailyExpenditure &date`/D16). Import = **skip-on-conflict** : une séance dont le `strongKey` existe déjà est **sautée** (ré-importer le même fichier ou un export plus récent recouvrant les mêmes séances → **0 doublon**). **`SCHEMA_VERSION` reste 2** (champ additif rétro-compat). `workouts`/`sets` **déjà dans `TABLES`** → D7 satisfait sans modif. Prouvé : `strongImport.test.mjs` (double import fixture → 28/504 identiques) + `migration.test.mjs` **S10** (base v6 réelle → bump v7 → données préservées, workout v6 sans `strongKey` survit, index unique présent et rejette un doublon).

- **`replace-on-conflict` DÉFÉRÉ** (report explicite, comme D17/D21 l'avaient été). Une séance **éditée dans Strong** après un 1er import (même `strongKey`, plus de séries) est aujourd'hui **ignorée** au ré-import (skip). La règle « delete+reinsert pour ce `strongKey` » sera tranchée si le besoin réel apparaît — pas un trou.

- **DURCISSEMENT A — parsing date SAFARI-SAFE.** « 2025-12-30 20:57:41 » **n'est pas ISO** (espace, pas de `T`/`Z`) → `new Date(chaîne)` renvoie **Invalid Date sur WebKit/iOS** alors que ça passe en node/Chrome. **INTERDIT** de passer la chaîne brute au constructeur : `date` = **slice des 10 premiers caractères** ; `startedAt` = **parse manuel par composants** → `new Date(année, mois-1, …)` avec des **nombres** (constructeur composants, sûr tous moteurs). Commentaire d'interdiction dans `strongImport.js` + test dédié (`strongImport.test.mjs` bloc D).

- **DURCISSEMENT B — filtre STRUCTUREL, pas littéral.** Une ligne est une **série ssi `Ordre de la série` est NUMÉRIQUE** (`/^\d+$/`). Les non-numériques sont écartées **ET comptées par libellé** dans le rapport (attendu fixture : **504 × « Minuteur de repos »**, 0 autre). Un libellé inconnu un jour est **visible**, jamais avalé. (Le filtre ne hardcode pas « Minuteur de repos ».)

- **Mapping colonnes FR + EN** : table de synonymes, détection à l'ouverture, erreur claire (`StrongImportError 'unknown-format'`) si une colonne requise (`date, workoutName, exercise, setOrder, weight, reps`) manque. Noms d'exercices **trimmés** (espaces traînants réels). Réps en flottants → coercition entière ; poids kg décimale point.

**Pourquoi.** L'export Strong est la source de vérité des perfs ; l'import doit être **idempotent** (Strong exporte tout l'historique à chaque fois) et **honnête sur la donnée** (profilé sur le vrai fichier, pas un format supposé). Tables existantes réutilisées (zéro nouvelle entité) ; bump réduit au strict minimum (un index pour le verrou d'idempotence). Aucune décision D1→D21 rouverte (D16 versioning additif, D7 export, D20 précédence consommé, D21/option C trained — tous respectés). Verrouillé par `strongImport.test.mjs` (parsing fixture réelle, mapping FR/EN, date Safari-safe, idempotence), `migration.test.mjs` **S10**, et un smoke (import réel → 28/504 → ré-import 0 doublon → reload).

## D23 — Analyse de perf : métrique PAR EXERCICE (e1RM plafonné OU volume étiqueté), N=3, ±2,5 %, 100 % dérivé, échauffement seam, variantes statiques

Phase 2, point 7 ROADMAP. Analyse des séries importées (D22) → progression / stagnation / régression + records + échauffement + variantes. **Calé sur l'export RÉEL** (`strong-export-reel.csv`, profilé côté analyse : 52 exos force, distribution des séances ≥2/≥3/≥5 = 26/18/8, reps très étalées 4-30, 4 cardio). Choix actés :

- **DURCISSEMENT DUR — MÉTRIQUE PAR EXERCICE, JAMAIS PAR SÉANCE.** Un exercice est suivi **À L'E1RM** (Epley `W×(1+reps/30)`, single `reps=1` → charge brute) **ssi ≥ MIN_SESSIONS (3) de ses séances ont un top-set VALIDE** (≥1 série à `reps ≤ EPLEY_REP_CAP (12)` ; le top-set = **max e1RM parmi les séries valides**, pas la charge max brute). **Sinon l'exercice ENTIER bascule AU VOLUME** (Σ poids×reps par séance) si ≥3 séances ont un volume > 0, **étiqueté « volume »** en synthèse ET en détail. **INTERDIT** de comparer un e1RM à un volume dans la même tendance. Une **séance sans valeur** pour la métrique retenue est **exclue** du calcul ET **ne compte pas dans la fenêtre N**. _Le bodyweight (W=0 → volume 0, aucune valeur) tombe en « insuffisant » — pas de 3ᵉ métrique « reps » qui violerait l'interdit de mélange ; tous les bodyweight de la fixture sont 1-session de toute façon._

- **GATE D'HONNÊTETÉ (calque règle `unknown` D21).** Sous 3 séances **avec valeur** pour la métrique retenue → verdict **« données insuffisantes »**, JAMAIS une tendance extrapolée sur 2 points. Affiché en **section repliée** (jamais masqué). Sur la fixture réelle : **16 exos suivis (tous e1RM), 0 volume naturel, 32 insuffisants, 4 cardio** — la voie VOLUME n'est donc déclenchée par **aucun** exo réel ≥3 séances (tous ont un top-set valide) → testée sur une **série synthétique haut-reps** (test anti-mélange dédié), l'honnêteté l'emporte sur le faux-couvert.

- **FENÊTRE de tendance = N SÉANCES de l'exo (jamais calendaire).** `STAGNATION_WINDOW = 3`. *Justif sessions ≠ jours* : l'overload est par **stimulus** ; 1×/sem vs 3×/sem ne doit pas changer le verdict. N=3 groundé : rend 18 exos analysables, N=5 n'en laisserait que 8 sur ~1,5 mois.

- **VERDICT ±2,5 %, robuste au bruit.** État courant = **MAX des 2 dernières séances** (absorbe une séance molle isolée) comparé au **MAX des séances antérieures de la fenêtre**. `ratio > 1,025` → ↑ progresse ; `< 0,975` → ↓ régresse ; entre → → stagne. Une séance molle isolée en dernier ne déclenche **jamais** une régression (prouvé en test : 108 puis 95 → reste ↑).

- **ÉCHAUFFEMENT = montée en charge dérivée du top-set de la DERNIÈRE séance.** Schéma par défaut `WARMUP_SCHEME = [40%×8, 60%×5, 75%×3, 90%×1]` (**constante UNIQUE exportée = seam** ; l'utilisateur la remplace par la sienne sans refonte). Arrondi `LOAD_INCREMENT = 2,5 kg` ; **drop** d'un palier dont l'arrondi est ≤ au précédent (collision) ou ≥ charge de travail ; sous `WARMUP_MIN_LOAD = 20 kg` ou charge nulle (bodyweight/cardio) → **N/A** (pas d'échauffement). On rampe vers une **CHARGE** (poids max travaillé), pas vers un nombre d'e1RM.

- **100 % DÉRIVÉ — AUCUN stockage, AUCUN bump Dexie** (`DEXIE_VERSION` reste **7**, `SCHEMA_VERSION` reste **2**). `src/lib/perf.js` = fonctions **pures** (calque `glycemic.js`), recalculées au montage de Perf (≤ ~500 sets = trivial). L'index `[exercise+date]` (D22) suffit au regroupement. Un dérivé stocké créerait une source de vérité concurrente à invalider à chaque import → rejeté. `backup.js` intouché (rien de nouveau à exporter).

- **VARIANTES anti-plateau = constante de code** `src/data/exerciseVariants.js` (donnée de **référence** statique, locale, v1 — comme `ingredientsSeed`). Lookup normalisé trim+casse → `[]` si inconnu (jamais d'erreur). **PAS d'IA** (Phase 3, rien codé ici).

- **CARDIO exclu de l'analyse force** (`classifyModality` D22 = keyword-only cycling/walking/running/rowing) : label explicite « suivi cardio hors v1 », aucun e1RM/volume. _Note : les mouvements de mobilité FR (Étirement, Squat PDC…) sont classés `strength` W=0 par le keyword-only → ils tombent en bodyweight → insuffisant (tous 1-session)._

- **UI Perf : segmented control `Synthèse | Import`** (la vue Import/log D22 **intacte**, déplacée sous l'onglet). Synthèse = liste des exos suivis (verdict ↑/→/↓ + valeur + badge PR) + section repliée « Données insuffisantes (N) ». Détail exo (tap) = **sparkline SVG inline** (zéro dépendance — contourne la dette Recharts au lieu d'y ajouter) + historique par séance + échauffement calculé + variantes.

**Pourquoi.** L'analyse est **gratuite côté données** (D22 a tout capté) ; il ne manquait que le calcul + l'affichage, purs et dérivés. Le durcissement « métrique par exercice » garantit qu'aucune flèche ne naît d'une comparaison e1RM↔volume incohérente. Tout est groundé sur la **distribution réelle** de la fixture, pas sur un gymrat théorique : seuils (REP_CAP 12, N=3, ±2,5 %, plancher échauffement 20 kg) calés sur les plages observées. Aucune décision D1→D22 rouverte (D7 export N/A, D16 aucun bump, D21 `trained` non touché, D22 schéma réutilisé tel quel). Verrouillé par `perf.test.mjs` (Epley + plafond, choix métrique sur fixture, gate insuffisance, **test anti-mélange synthétique**, verdict ±2,5 % + séance molle, PR, échauffement, variantes, **exercices assistés**) + un smoke (import → Synthèse → verdict exo réel + section insuffisante + log import accessible). Différé (porte ouverte) : timing horaire des glucides autour de la séance (`startedAt` capté, exploitation future), `replace-on-conflict` import (D22), couche IA des variantes (Phase 3).

**ADDENDUM D23 — exercices ASSISTÉS : lecture du verdict INVERSÉE (contresens sémantique corrigé).** Dans Strong, le « poids » d'un exercice assisté (`Chest Dip (Assisted)`, `Pull Up (Assisted)`…) = la charge d'**ASSISTANCE** → sémantique **inversée** : moins de poids = moins d'aide = **plus fort**. Vérifié sur la fixture (`Chest Dip (Assisted)` : assistance e1RM 86 → ~30-39 ; `Pull Up (Assisted)` : 69 → 53 = progression réelle), le verdict initial « Régresse » était un **contresens**. Correctifs : (1) détection keyword `ASSISTED_KEYWORDS = ['assisted','assisté','assistée']` (`isAssisted`, même mécanique que `classifyModality`, trim+casse) ; (2) **la LECTURE du verdict s'inverse** (`invertVerdict` : assistance ↓ → ↑ progresse ; ↑ → ↓ ; → inchangé ; bande ±2,5 % et **métrique e1RM inchangées** → l'interdit de mélange D23 n'est PAS touché) ; (3) **PR assisté = MINIMUM d'assistance all-time** (pas le max ; `personalRecords({assisted})` → `best=min`, `loadExtreme=min`) ; (4) **échauffement N/A** (monter en charge vers une assistance n'a pas de sens — même rendu que bodyweight/cardio) ; (5) **étiquette UI explicite** sur la carte (« assisté — charge = assistance ») + note de détail (« moins = plus fort, lecture inversée ») + sparkline « assistance (↓ = mieux) » pour que la valeur ne soit jamais lue comme une charge soulevée. Verrouillé par `perf.test.mjs` bloc **M** (détection, `invertVerdict`, Chest Dip réel : verdict brut `down` → lu `up`, PR = min, échauffement N/A) + smoke amendé (Chest Dip → **« Progresse »**, label assisté, échauffement « Sans objet »).

## D24 — Suggestions de repas : carte Jour dérivée des recettes (D19), score pondéré seam, gate honnête + complément mono-ingrédient, chips dérivées zéro-bump

Phase 3 point 8 ROADMAP **REDÉFINI** : le « mini-chat repas » est **abandonné comme surface**. Pas de chat, pas de champ texte libre, pas de parsing de mots-clés (la conversation = couche IA Phase 3, différée). La surface = une **carte « Suggestions »** sur l'écran Jour + des **chips de filtres**. `src/lib/suggest.js` (PUR, calque `glycemic.js`/`perf.js`, testé node, déterministe). Choix actés :

- **Source PRIMAIRE = les recettes enregistrées (`recipes`, D19), exactes au gramme par construction.** PAS de scraping, PAS de seed de recettes externes. Les macros d'une recette sont **dérivées à la volée** par `resolveRecipe` + `composeTotals` contre la biblio **courante** (formule vivante D19) — jamais un snapshot. Une recette dont **≥1 ligne ne résout plus** (ingrédient supprimé) est **EXCLUE** du scoring (`recipeMacros.resolvable=false`) : on ne score pas des macros incomplètes. **ZÉRO stockage, ZÉRO bump Dexie** (`DEXIE_VERSION` reste 7, `SCHEMA_VERSION` reste 2) — recalcul au montage du Jour. `backup.js` intouché.

- **ENTRÉES (toutes des seams actés, rien de neuf en base)** : restants du jour = cibles D15 − `effectiveConsumed` (D20) ; contexte séance = `effectiveTrained` (D21/D22) → **pondération** du tri (jour d'entraînement remonte les recettes haut-IG, jour de repos les descend — **jamais un filtre dur**) ; heure → **fraction du budget restant visée** (`FILL_FRACTION_BY_SLOT` ; le matin laisse de la marge, le soir clôt la journée). **Bornes des créneaux = `SLOT_BOUNDS` exporté** (aucun seuil horaire enfoui). ORIENTATION LENDEMAIN : si la **veille** s'est finie en **carence protéique** (`< PROTEIN_DEFICIT_RATIO·cible`, défaut 0,80), le protéiné est **sur-pondéré** aujourd'hui (`proteinCarry`). **ORIENTATION SEULEMENT** : aucune compensation calorique, **le budget du jour ne change JAMAIS**. Garde : veille **sans journal détaillé** (mode manuel D20 / 0 entrée) → `yesterdayProtein=null` → **pas de boost, jamais de NaN**.

- **SCORE = seam unique pondéré** (`SUGGEST_WEIGHTS`, recalibrable sans refonte, calque `WARMUP_SCHEME`). Faisabilité binaire (alimente le gate) : une recette « tient » ssi `kcal ≤ restant·(1+KCAL_OVER_TOL=0,10)` ET `sucresSimples ≤ budget sucres` (ligne dure recomp, borne stricte) — un dépassement protéines/glucides n'est **pas** un veto. Tri parmi les faisables : `protein·proteinFit + kcal·kcalFit − sugars·sugarsCost + giMod + carryMod`, avec `proteinFit = clamp(rp/Rp, 0, 1,5)` (0 si plus de besoin protéique), `kcalFit = 1 − |kcalVisé − rk|/kcalVisé` (pénalise sous ET sur-comblement). **Poids validés** : protéine×3, kcal×1, sucres×2, gi±1, carry×1,5, cap proteinFit 1,5.

- **MODE DÉGRADÉ D20** (`consumedFromManual` : 0 entrée, total kcal manuel → macros consommées **inconnues**) : score **kcal-only** (`kcalFit` + séance), faisabilité kcal seule (sucres ignorés), **aucune affirmation macro**, pas de complément (le gap protéique est inconnu).

- **GATE D'HONNÊTETÉ (non négociable)** : si AUCUNE recette ne tient alors qu'il reste du budget exploitable → message **chiffré explicite** (`Il reste X kcal, Y g de protéines, Z g de sucres — aucune recette ne tient`), **JAMAIS un « moins pire » silencieux**. + **fallback complément mono-ingrédient** (`proteinFiller`, pur) : l'ingrédient le plus **dense en protéines** (portion la plus petite) couvrant le restant protéique dans la borne `FILLER_MAX_G` (300 g) et le budget kcal ; arrondi `FILLER_GRAM_STEP` (5 g) ; `null` si rien ne tient honnêtement. **Étiqueté « complément », jamais « repas ».**

- **ÉLARGISSEMENT DU GATE (point 3 acté)** : le complément s'affiche **AUSSI dans l'état sous-seuil** (`restant kcal < MIN_SUGGEST_KCAL=150`) **si le gap protéique restant ≥ `FILLER_MIN_PROTEIN_GAP` (15 g)** — LE cas du soir (140 kcal restants, 25 g de protéines à caser → la carte ne disparaît pas muette). Sous le seuil **sans** gap protéique → carte **masquée**. Pas de complément en secondaire permanent (chemin gate/sous-seuil uniquement).

- **CHIPS = taxonomie minimale DÉRIVÉE, v1 ZÉRO bump** : `Protéiné` (≥`PROTEIN_CHIP_SHARE`=30 % des kcal en protéines), `Léger` (≤`LIGHT_CHIP_KCAL`=300 kcal), `Post-séance` (highShare ≥ `HIGH_GI_REST_SHARE`, **réutilise le seam glucidique D21**). Toutes calculées depuis les macros résolues → **aucun champ `tags`, aucune migration**, et surtout **aucune surface d'édition de recette** (que D19 a justement différée). Filtre **client** sur la liste scorée. **DIFFÉRÉ explicite** : champ `tags` + « type de plat » (petit-déj/midi/soir) + « rapide » (non dérivables → exigeraient un bump v8 additif D16 + édition de recette) — **non construit ici**.

- **UI** : carte sur le Jour, **sous le bloc macros/sucres** (l'enchaînement « voilà ton restant → voilà quoi manger »), au-dessus de la séance. **2-3 suggestions max** (`MAX_SUGGESTIONS`). **1 tap = `applyRecipe` (flux D19 EXISTANT réutilisé, jamais dupliqué)** → append au journal → le Jour se recharge (`refreshKey`) → `effectiveConsumed` recalcule → la carte se re-trie (budget réduit). **Quand** : visible seulement si `restant ≥ MIN_SUGGEST_KCAL` (sinon état sous-seuil : complément si gap protéique, sinon masquée). États : `no-recipes` (nudge vers Bouffe) / `suggestions` / `gate` (+ complément) / `filler-only` (sous-seuil + gap) / `hidden`.

- **PÉRIMÈTRE v1** = moteur + carte + chips dérivées + gate + complément + orientation lendemain. **RIEN d'autre.** **DIFFÉRÉS inscrits** : couche IA conversationnelle (Phase 3) ; import de recette par texte collé type légende Insta/TikTok (Phase 3, cas IA) ; seed de recettes externes (décision séparée, probablement jamais en masse) ; champ `tags`/type-de-plat (bump v8 si jugé nécessaire plus tard).

**Pourquoi.** Le besoin « qu'est-ce que je mange ce soir dans mon budget » se sert **gratuitement côté données** : les recettes (D19) sont des compositions exactes, les restants/séance sont déjà des seams (D15/D20/D21/D22). Il ne manquait qu'un calcul pur + une carte. Faire des recettes la source primaire (vs scraping/IA) garde le 100 % local et le déterministe. Le gate honnête (jamais un « moins pire » muet) + le complément calculé respectent la discipline anti-hallucination du projet. Aucune décision D1→D23 rouverte (D7 export N/A — rien de nouveau ; D16 aucun bump ; D19 recettes/`applyRecipe` réutilisés tels quels ; D20 `effectiveConsumed` non touché ; D21 seam glucidique réutilisé ; D22 `effectiveTrained` réutilisé). Verrouillé par `suggest.test.mjs` (jour vide / budget large / budget serré / rien ne tient → gate chiffré / sucres liants / jour d'entraînement remonte le haut-IG, repos le descend / carence veille sur-pondère le protéiné / complément couvre-borne-`null` / recette ligne morte exclue / mode dégradé D20 kcal-only / **sous-seuil + gap → complément visible** / **sous-seuil sans gap → masquée**) + un smoke (recette dans le budget → carte → 1 tap → journal → budget réduit + re-tri).

## D25 — Boissons : modèle PORTION + kcal PORTÉ jamais recalculé + classification 2 axes + zéro bump + seed flag

Lot Boissons (déféré depuis D18) : recréation de la base ~38 boissons alcoolisées (ancien projet perdu) + saisie au journal. **Périmètre v1 STRICT** = base + saisie + intégration budget/sucres + ligne alcool. **PAS de stats alcool, PAS de tendance conso.** Choix actés :

- **MODÈLE = PORTION standard, pas une pesée /100 g.** Une boisson = une portion (`33 cl` bière, `12 cl` vin, `4 cl` spiritueux…) avec des valeurs **ABSOLUES par portion** (`{ kcal, protein, carb, sugarsSimple, fat, gi, portionLabel, portionMl, alcoholG }`). Saisie = `logDrink(drink, count)` → 1 `journalEntry` `sourceType:'drink'` (D2, réservé depuis v2, **enfin utilisé**) avec macros × count **FIGÉES** (D1). Le multiplicateur (« 2 bières ») s'applique au log ; `nameSnapshot` porte la portion (« Bière blonde (33 cl) ») pour que l'édition de quantité reste interprétable ; `grams = portionMl × count` (ml ≈ g) → édition/suppression via le **chemin journal standard** (`updateEntryGrams`/`deleteEntry`, regram du snapshot, jamais relu).

- **INVARIANT DUR — le kcal d'une portion est TOUJOURS PORTÉ tel quel, JAMAIS recalculé depuis les macros.** L'alcool fait **7 kcal/g** : `kcal ≠ 4P + 4G + 9L`. Un recalcul depuis les seules macros perdrait les calories de l'alcool. Au seed, le `kcal` est **dérivé de la composition réelle** (`round(7·alcoholG + 4·(carb+protein) + 9·fat)`) puis stocké et porté. **Aucun code existant ne recalcule** un kcal depuis les macros (audité : `Jour.jsx` agrège `e.kcal` tel quel ; `regramMacros` scale le kcal **stocké** ; `composeTotals`/`lineMacros` ne servent que les ingrédients /100 g). Prouvé : `drinks.test.mjs` (vodka kcal 88, `4P+4G+9L = 0 ≠ 88` ; survit au log ET au regram).

- **CLASSIFICATION = 2 AXES SÉPARÉS (jamais mélangés, calque D21)** : `sugarsSimple` = sucres simples réels → **budget recomp** (alerte A, < 20 g/j) ; `gi ∈ {low,mid,high}` = classe glycémique → **composition IG du jour** (alerte B) ; `alcoholG` = **porteur du kcal** (≠ macro, jamais sommé aux macros). Table : spiritueux secs `carb 0` → poids nul en compo (auto-neutre) ; vins **secs** `low`, **demi-sec/moelleux/doux** `mid` (**champagne/mousseux BRUT = sec → `low`**, dosage < 12 g/L) ; **bière `gi:high`** (maltose) **mais sucres bas** → frappe B pas A — **`gi:high` de la bière ASSUMÉ** (alerte B un soir de repos défendable en recomp ; reclassable au seed si bruyant, sans toucher la logique) ; cocktails/liqueurs sucrés `high` + sucres élevés (frappent A et B), liqueurs crémeuses portent `fat`.

- **LIGNE ALCOOL (Jour) = résidu DÉRIVÉ, jamais stocké ni indexé.** `alcoholKcal(entries)` = `Σ max(0, kcal − (4P+4G+9L))` sur les **seules** entrées `sourceType:'drink'`, arrondi. Capture honnêtement la part calorique qu'aucune macro n'explique (≈ 7·alcoholG) **sans champ supplémentaire** au snapshot. Affichée « Alcool : X kcal (non répartis) » **uniquement si X > 0** ce jour (aucune boisson → ligne absente). Conséquence assumée et honnête : ces kcal comptent dans le **restant/consommé** (via `e.kcal`, D20) mais les barres P/C/L ne les répartissent pas (l'alcool n'est pas une macro). Prouvé : `drinks.test.mjs` (résidu, ignore les entrées ingrédient, 0 sinon) + smoke (bière ×2 → 182 kcal ; absente avant toute boisson).

- **EXCLUSION SUGGESTIONS prouvée par CONSTRUCTION (D24).** Une boisson n'est **jamais** suggérée comme repas : `suggest.js` n'importe pas `drinks` ; la source D24 = `recipes` résolues contre la biblio **ingrédients** + `proteinFiller` puisé dans `ingredients`. Une boisson vit dans le store `drinks` (jamais passé à `suggestMeals`) et ne peut entrer dans une recette (D19 ingrédients-only, `saveMeal`/`applyRecipe` n'écrivent que `sourceType:'ingredient'`). Forcer une recette à référencer un id de boisson → résolution échoue → recette **exclue** (comme une ligne morte). Prouvé : `suggest.test.mjs` bloc **O**.

- **SCHÉMA INCHANGÉ — ZÉRO bump Dexie** (`DEXIE_VERSION` reste **7**, `SCHEMA_VERSION` reste **2**). Le store `drinks` existe depuis v2, est **déjà dans `TABLES`** (export/import couvert, D7 satisfait sans modif, `backup.js` intouché), ses index `name, category` suffisent, et les champs portion ajoutés (`gi, fat, protein, portionLabel, portionMl, alcoholG`) sont **NON INDEXÉS** → écriture libre. **Calque EXACT de D18** : un bump n'aurait servi qu'un nouvel **index**. Donc **pas de test S_n** ; à la place, round-trip export/import (`drinks.test.mjs`) prouve que la base + une entrée boisson survivent à une restauration.

- **SEED = fichier committé `src/data/drinksSeed.js` (38 boissons, ids slug stables) + flag `settings.drinksSeededV1`.** Calque exact de `seedLibraryIfNeeded`/`librarySeededV1` (D18) : part sur device déjà initialisé ; le flag vit dans `settings` → **voyage avec l'export/import** (backup post-seed ne re-seede pas) ; **ceinture + bretelles** insert-only-missing (re-run = ni doublon ni throw, n'écrase jamais une boisson éditée). **Enrichissable** (custom = UUID `newRow`, `isCustom:true`). Valeurs **régénérées depuis des standards** (ordres de grandeur réalistes, pas de précision inventée) ; **sanity test** (`drinks.test.mjs`) : `kcal ≈ 7·alcoholG + 4·(carb+protein) + 9·fat` ±10 sur les 38 → attrape une valeur fabriquée aberrante.

- **UI** : 5ᵉ onglet **« Boissons »** dans le segmented control de Bouffe (texte verbatim → smokes existants intacts ; `text-[11px]` → les 5 onglets **tiennent sans clipping** sur 390px, vérifié par capture, **pas de fallback** nécessaire). Liste recherche + filtres catégorie dynamiques + stepper ×N + bouton « Boire » (1 tap) ; CRUD custom calque la Bibliothèque. La ligne alcool vit sur le **Jour** (sous sucres).

**Pourquoi.** Besoin réel « entrer un verre dans mon budget » avec une donnée honnête sur l'alcool. **Table dédiée existante réutilisée** (zéro nouvelle entité, zéro bump) ; le `sourceType:'drink'` (D2) prévu de longue date est enfin câblé → le consommé/sucres/compo IG du jour **digèrent une boisson gratuitement** (agrégation `journalEntries` déjà câblée, D20/D21). L'invariant kcal-porté + la ligne « non répartis » respectent la discipline anti-hallucination (pas de fausses macros, pas de kcal inventé). **Aucune décision D1→D24 rouverte** (D1 figeage, D2 sourceType, D5/D21 gi, D7 export, D16 zéro bump, D18 seed-flag, D19 recettes ingrédients-only, D20 effectiveConsumed, D24 exclusion). Verrouillé par `drinks.test.mjs` (portion ×N, **invariant kcal porté**, alcoholKcal, axes séparés, validateDrink, sanity 38, seed flag idempotent, round-trip), `suggest.test.mjs` bloc **O** (exclusion), et un smoke (seed 38 → bière ×2 → 282 mangé + ligne alcool 182 → persiste). **Différés inscrits** : stats/tendance alcool ; boisson-dans-recette ; classification fine par marque.

## D26 — Navigation (5 onglets, Journal promu / Chat retiré) + tableau de bord à anneaux dérivés (honnêteté des moyennes) + lien kcal

Lot **Navigation + Tableau de bord** (UX, intercalé avant le point 9). Périmètre v1 STRICT = **A** (Journal promu en nav) + **B** (kcal cliquable) + **C** (anneaux jour/semaine/mois). **RIEN d'autre** (pas de partage, pas d'historique annuel, pas de notifications). Choix actés :

- **A — NAV À 5 ONGLETS, « Chat » RETIRÉ, « Journal » PROMU.** La barre passe de `Jour · Bouffe · Poids · Perf · Chat` à **`Jour · Journal · Bouffe · Perf · Poids`**. Le placeholder **« Chat » est supprimé de la nav** : il est **doctrinalement périmé depuis D24** (le mini-chat a été **abandonné comme surface** ; la conversation = couche IA Phase 3 point 9). La surface du point 9 sera **conçue au point 9**, pas portée par un onglet vide entre-temps. Le **Journal du jour quitte les sous-onglets de Bouffe** (qui redescend à **4** : `Composer · Bibliothèque · Recettes · Boissons`) pour devenir un **écran nav autonome** (`src/screens/Journal.jsx`, charge `journalEntries` du jour à son montage ; édition/suppression inchangées). Composer `onSaved` **navigue** vers l'onglet Journal (`onNavigate`, prop App→Bouffe) ; Recettes/Boissons ne naviguent pas (bandeau de retour conservé). **Capture 390px de la nav finale** (méthode D25) : les 5 labels (dont « Journal », 7 car. ≫ « Chat », 4 car.) **tiennent sans clipping ni wrap**, `text-[10px]` icône empilée — **pas de fallback**. _Code mort retiré : `Placeholder`/`LABELS`/`PHASES` (Chat seul les utilisait)._

- **B — « X mangé » du héro = SEUL élément cliquable → onglet Journal.** Le total consommé (le « X mangé » sous le restant, pas le gros chiffre qui est le *restant*) devient un **lien** (souligné pointillé discret + `aria-label="Voir le détail de la journée"`) ; tap → `setTab('journal')` (prop `onSeeJournal` App→Jour). La cible « état détaillé de la journée » **est** l'écran Journal promu en A — couple A+B sans nouvelle vue.

- **C — 3 ANNEAUX CÔTE À CÔTE + toggle JOUR/SEMAINE/MOIS, 100 % DÉRIVÉ.** `src/lib/dashboard.js` (PUR, testé node, calque `glycemic`/`perf`/`suggest`) ; l'I/O (charger la fenêtre) vit dans `<DashboardRings>` (Jour.jsx) via **une range-query indexée `date` par table**. **ZÉRO stockage, ZÉRO bump** (`DEXIE_VERSION` reste 7, `SCHEMA_VERSION` reste 2 ; `backup.js` intouché). Rendu **SVG inline** (calque `EnergyRing`/sparkline D23) — **aucune dépendance**. Concentrique façon Apple **non retenu** (illisible avec un compteur « N j saisis » par anneau) → **polish cosmétique différé**, non construit.
  - **Anneau 1 Énergie** = consommé (seam `effectiveConsumed` D20) / `targetKcal`. **Anneau 2 Protéines** = protéines / `targetProtein`. **Anneau 3 Dépense** = dépense saisie / **TDEE dérivé** (`computeTargets(profile, dernier poids).tdee`, jamais stocké) + **compteur séances** sous l'anneau.

- **HONNÊTETÉ DES MOYENNES (le cœur, non négociable).** Un jour **SANS donnée est EXCLU du dénominateur, JAMAIS compté 0** (un 0 fantôme effondrerait la moyenne). Chaque anneau porte son **propre `N j saisis`** ; les N **diffèrent** et c'est assumé/affiché. Règle exacte par anneau :
  - **Énergie** : jour « saisi » = ≥1 entrée journal **OU** total manuel D17. Le verrou nullish D20 survit (0 manuel = total réel).
  - **Protéines** : jour « saisi » = ≥1 entrée **détaillée** uniquement. Un jour **mode-manuel D20** (macros inconnues) est **EXCLU** (ne pollue pas l'anneau macro), pas compté 0 → **N₂ ≤ N₁** assumé et affiché.
  - **Dépense** : jour « saisi » = ligne `dailyExpenditure` présente → « moy. 412 kcal/j **sur 4 j saisis** ». Jamais un jour vide à 0.
  - **JOUR** : un anneau sans donnée affiche **« non saisi »**, JAMAIS « 0 % » (vérifié par smoke). SEM/MOIS sans donnée → « aucune donnée ».
  - **Compteur séances** : via le **seam `effectiveTrained` (D21/D22)** — `trainingDays` (toggle manuel) **OU** séance importée RÉELLE ce jour (échauffement seul exclu, denylist). **JAMAIS `trainingDays` brut** : un jour avec séance Strong importée **sans toggle manuel COMPTE** (test node dédié).
  - **HÉRO INCHANGÉ — non-décision ACTÉE (qu'aucune session future ne « répare »).** La règle d'honnêteté ci-dessus protège les **moyennes** (anneaux), PAS les **compteurs live**. L'anneau « budget » du héro et son « X mangé » restent un **COMPTEUR TEMPS RÉEL** : « 0 mangé » / « 0 % budget » à 8h est **factuel** (rien consommé encore), pas un trou de donnée. Les anneaux sont des **STATISTIQUES SUR JOURS SAISIS** où un 0 fantôme serait un **mensonge de dénominateur** → ils affichent « non saisi ». Les deux sémantiques (compteur live vs stat) sont **distinctes par nature et assumées** ; la divergence visuelle (héro « 0 % » vs anneau Énergie « non saisi » un jour vide) est **voulue, pas une dette différée**.

- **SÉMANTIQUE DES PÉRIODES = fenêtre GLISSANTE** : semaine = `[today-6 … today]` (7 j), mois = `[today-29 … today]` (30 j), dates locales `todayKey`, **construction par composants** (`new Date(y, m-1, d-i)` → rollover mois/année correct ET sûr tous moteurs, cf. D22 ; jamais `new Date(chaîne)`). **Justif** : cohérent avec `movingAverage` du poids (D14, trailing) ; toujours une fenêtre pleine (pas de « lundi/1ᵉʳ du mois remis à zéro quasi vide ») ; aucune dépendance au 1ᵉʳ jour de semaine. **Alternative calendaire** (ISO-semaine / mois civil) **notée NON retenue** (plus « parlante » mais fenêtres partielles trompeuses ; la règle « jours saisis seulement » rend de toute façon le calendaire peu sensible).

- **SMOKES INVENTORIÉS AVANT DE TOUCHER LA NAV.** 1 seul cassait dur : la pesée (`name:'Jour'` **sans `exact`**) — depuis que « Journal » est en nav permanente, `'Jour' ⊂ 'Journal'` → 2 matches → corrigé en **`exact:true`**. Les flux `Bouffe → Journal` (nutrition, recettes) **nettoyés** (un seul clic nav `Journal`). Les boutons de période portent un `aria-label="Période jour/semaine/mois"` (≠ texte « Jour ») pour **ne pas entrer en collision** avec l'onglet nav « Jour ».

**Pourquoi.** Le besoin « voir d'un coup où j'en suis (jour/semaine/mois) » se sert **gratuitement côté données** : consommé/protéines/dépense/séance sont déjà des seams (D15/D20/D21/D22). Il ne manquait qu'un calcul pur + 3 anneaux. La discipline anti-hallucination impose l'**honnêteté des moyennes** : une dépense saisie 4 jours sur 7 doit afficher « moy. /j sur 4 j saisis », jamais une moyenne diluée par des 0 inventés — même règle, plus stricte, côté protéine (un jour manuel-only n'a pas de macro à moyenner). **Aucune décision D1→D25 rouverte** (D2 journal, D7 export N/A, D13/D16 zéro bump, D17/D20 consommé, D21/D22 `effectiveTrained`, D24/D25 inchangés). Verrouillé par `dashboard.test.mjs` (fenêtre glissante + rollover ; jour vide exclu ≠ 0 ; manuel-only N₂<N₁ ; dépense « sur N j » ; période vide → `hasData:false` ; **séance workouts-only comptée**) + 2 smokes (nav+lien ; anneaux « non saisi » + SEMAINE « 1 j saisis » prouvant l'exclusion des jours vides). **Différés inscrits** : polish concentrique ; historique annuel ; sémantique calendaire (porte ouverte).
